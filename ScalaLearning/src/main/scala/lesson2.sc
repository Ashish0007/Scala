class Foo{}

object FooMaker{
  def apply = new Foo
}

//val fooMaker = new FooMaker()

(x :Int) => x+1

val f = (x:Int) => x+1

1.to(10)

BigInt.probablePrime(4,util.Random)

"Ashish".distinct

10 max 2
BigInt(2).pow(1024)